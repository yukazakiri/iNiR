#!/usr/bin/env bash
# Compatibility shim for installs that still source this historical file.
# iNiR no longer overrides glibc allocator policy; Quickshell/Qt resource
# lifetime is managed by the owning QML components instead.
